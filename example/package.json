{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "rm -rf deploy && spago build && spago bundle-app --main Main --to build/index.js && parcel build static/index.html --no-cache --dist-dir deploy && http-server ./deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.19",
    "http-server": "^14.1.1",
    "parcel": "^2.8.3",
    "purescript": "^0.15.9",
    "spago": "^0.21.0"
  },
  "dependencies": {
    "preact": "^10.13.2"
  }
}
